FROM json USE loads, dumps END

s = read_file("tests/json_numbers.json")
v = loads(s)
PRINT dumps(v)
IF v["ints"][1] != -1: RAISE("numbers: int mismatch") END
IF v["floats"][0] != 1.5: RAISE("numbers: float mismatch") END
PRINT "OK: json_numbers"
