Run the minimal API server (scaffold):

```bash
python3 services/api/server.py services/api/config.json
```

By default the server will not start if `online` is `false` in the config. Edit `services/api/config.json` or use the GUI Save button to change settings.
