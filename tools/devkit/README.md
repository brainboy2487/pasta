# Pasta Devkit

Developer toolkit for the Pasta project. Contains scripts and stubs for:
- building and testing
- safe patch templates
- UI primitives (spinner, progress bar)
- smoke test runner and golden checks
- installer helper

Use `python3 tools/devkit/tasks.py` or the top-level `Makefile` targets.
